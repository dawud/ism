module Steel.ST.Util

open Steel.Memory

(* Trusted bootstrap adapter for Steel utility imports. The current scaffold
   does not expose utility operations here; this module exists to keep imports
   stable until the real Steel dependency is wired in. *)
